include <iostream>

using namespace std;
// 총 몇 분인지 구해서 계산함
int main() {
	int h, B, C, m;
	int cnt = 0;
	cin >> h >> B >> C;
	cnt = h * 60 + B + C;
	cout << (cnt/60) % 24 << " " << (cnt%60);

	return 0;
}
